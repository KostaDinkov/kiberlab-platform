import React from 'react';

export default function (props) {
    return (
        <div className={props.className}>
            <img src="./assets/logo.svg" alt="kiberlab logo"/>
        </div>
    )
}